'use strict';

//Pricing service used for articles REST endpoint
/*  Do NOT use!
angular.module('mean.pricing').factory('Pricing', ['$resource', function($resource) {
    return $resource('pricing/:pricingId', {
        pricingId: '@_id'
    }, {
        update: {
            method: 'PUT'
        }
    });
}]);  */